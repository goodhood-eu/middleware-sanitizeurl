{
  "name": "middleware-sanitizeurl",
  "description": "ExpressJS middleware that checks request URL for potentially breaking things",
  "author": "Nebenan.de <max@nebenan.de>",
  "license": "MIT",
  "readmeFilename": "README.md",
  "repository": "good-hood-gmbh/middleware-sanitizeurl",
  "bugs": "https://github.com/good-hood-gmbh/middleware-sanitizeurl/issues",
  "version": "2.0.2",
  "keywords": [
    "express",
    "expressjs",
    "middleware",
    "query",
    "querysting",
    "sanitize"
  ],
  "main": "index.js",
  "scripts": {
    "reinstall": "rm -rf node_modules package-lock.json && npm install",
    "test": "env TZ='Europe/Berlin' mocha || exit 0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "eslint": "^6.4.0",
    "eslint-config-nebenan-base": "^1.3.1",
    "mocha": "^6.2.2",
    "sinon": "^7.5.0"
  },
  "dependencies": {
    "debug": "^4.1.1"
  }
}
